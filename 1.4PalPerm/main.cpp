#include <iostream>
#include <string>

using namespace std;

bool palPerm(string input)
{
    if(input.length % 2 == 1)
    {
        //odd number means one letter does not repeat
        
    }
    else
    {
        //all letters will be twice
    }
}

int main()
{
    string test = "taoc cta";
}